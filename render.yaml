services:
  - type: web
    name: visioncine-api
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node server-cloudflare.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
